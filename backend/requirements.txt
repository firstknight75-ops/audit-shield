fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
python-magic==0.4.27
cryptography==43.0.1
redis==5.0.8
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==0.24.0
email-validator==2.2.0
celery==5.4.0
pytz==2024.1
pillow==10.4.0
pdf2image==1.17.0
pytesseract==0.3.13
orjson==3.10.7
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
prometheus-client==0.20.0
python-slugify==8.0.4
limits==3.13.0
structlog==24.4.0
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
prometheus-fastapi-instrumentator==7.0.0
aiofiles==24.1.0

# Export engine dependencies — imported eagerly via app.api.phase4.
# Bug #1 fix: without this, a clean `pip install -r requirements.txt`
# followed by `python -c "import app.main"` raises ModuleNotFoundError
# and the entire API fails to boot, not just the export feature.
openpyxl==3.1.5

# PDF export — falls back to HTML if not installed (already wrapped in
# try/except in app.exports.engine). Declared so a clean install doesn't
# silently miss the optional dep.
weasyprint==62.3

# Reporting watermarks — optional, falls back to "no overlay" if missing.
reportlab==4.2.5
pypdf==5.1.0
